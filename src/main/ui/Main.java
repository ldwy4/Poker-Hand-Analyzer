package ui;

import model.Card;
import model.Player;
import model.Table;

public class Main {
    public static void main(String[] args) {
        InfoManager info = new InfoManager();
        info.handleUserInput();
    }
}
